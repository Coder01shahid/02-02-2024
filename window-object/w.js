/*
alert("window object");
prompt("window object");
confirm("window object");
document

a lot more features which is used by js but these all are of window objects..
*/

//window-object , heap memory and stack memory are the part of browser context api
// and these are the features which are provided by browser to javascript/..